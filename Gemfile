source "https://rubygems.org"

# CocoaPods is required by Flutter's iOS build. Pinning it here so the version
# is reproducible across machines. Bump intentionally with `bundle update cocoapods`.
gem "cocoapods", "~> 1.16"
